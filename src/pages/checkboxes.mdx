import DefaultCheckbox from '../components/checkboxes/DefaultCheckbox'

# Checkboxes

Use the checkbox component inside forms, as links, social login, payment options with support for multiple styles, colors, sizes, gradients, and shadows. {{ className: 'lead' }}

The card component is probably the most widely used element in any user interface or website as it can be used to launch an action but also to link to other pages.

Opera8 provides a large variety of styles and sizes for the card component including outlined checkbox, multiple colors, sizes, checkbox with icons, and more.


## Default Checkbox

Use these default Checkbox styles with multiple colors to indicate an action or link within your website.
<DefaultCheckbox />