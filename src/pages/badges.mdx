import Badge from '../components/badges/Badge'

# Badges

Use the badges component inside forms, as links, social login, payment options with support for multiple styles, colors, sizes, gradients, and shadows. {{ className: 'lead' }}

The card component is probably the most widely used element in any user interface or website as it can be used to launch an action but also to link to other pages.

Opera8 provides a large variety of styles and sizes for the card component including outlined badges, multiple colors, sizes, badges with icons, and more.


## Badge

Use these default badge styles with multiple colors to indicate an action or link within your website.

<Badge />