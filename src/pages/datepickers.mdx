import DatepickerVariation from '../components/datepickers/DatepickerVariation'

# Datepickers

Use the Datepicker component inside forms, as links, social login, payment options with support for multiple styles, colors, sizes, gradients, and shadows. {{ className: 'lead' }}

The card component is probably the most widely used element in any user interface or website as it can be used to launch an action but also to link to other pages.

Opera8 provides a large variety of styles and sizes for the card component including outlined Datepicker, multiple colors, sizes, Datepicker with icons, and more.


## Datepicker Variation

Use these default Datepicker styles with multiple colors to indicate an action or link within your website.

<DatepickerVariation />